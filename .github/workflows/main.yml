name: My Workflow
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name : Test
        run: |
          echo $API_KEY
        env:
          API_KEY: ${{ secrets.OPENAI_API_KEY }}
